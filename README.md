# SAM-Agent-WhatsApp
SAM (Stateful Agent Model) is a production-oriented AI agent architecture with explicit orchestration, state management, and memory separation. It implements a WhatsApp interface while keeping the core system interface-agnostic, CI/CD-ready, and observable.
